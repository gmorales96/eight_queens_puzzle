language: python

python:
  - "3.9"

services:
  - docker

before_install:
  - docker-compose up --build -d

script:
  - docker-compose exec -T app pytest ./tests/test_queens_problem.py

after_script:
  - docker-compose down
  